
	
{% load mptt_tags %}
<ul class="root" id="tree">
{% recursetree nodes %}
  <li {% if node.level > 1 %}style="display:none;"{% endif %}>
  <input style="display:none;" type='checkbox' id="{{ node.id }}" value="{{ node.id }}" name='location'/><a id="anchor_{{ node.id }}" style="padding-left:5px;" onClick="edit({{node.id}})"  href='javascript:void(0)'>{{ node.name }}<img style="display:none;" border='0' onClick="delete({{node.id}})"  src='/static/simple_locations/images/delete.png'></img></a></input>
   <span></span>
   <script type="text/javascript" >
		children[{{ node.id }}] = [{% for child in node.get_children %}{{ child.id }},{% endfor %}];
		{% if node.parent %}
	
			parent[{{ node.id }}]="{{ node.parent.id }}";
		
		{% endif %}
	</script>
	{%if node.is_leaf_node%}
					<li><a style="font-size:12px;" href="javascript:void(0)" onClick="add({{node.id}})">Add New</a></li>
                {% endif %}
	{% if not node.is_leaf_node %}
                    <ul class="children">
                        {{ children }}
                    </ul>
					
                {% endif %}
   
   </li>
{% endrecursetree %}
<li><a style="font-size:12px;" href="javascript:void(0)" onClick="add({{node.id}})">Add New</a></li>
</ul>